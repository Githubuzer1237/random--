let randomNumber = Math.floor(Math.random() * 100) + 1
const guessInput = document.querySelector('#guessInput')
const sumbitBtn = document.querySelector('#sumbitBtn')
const result = document.querySelector('#result')


sumbitBtn.addEventListener('click', () => {
    const userGuess = Number(guessInput.value)

    if (!userGuess || userGuess < 1 || userGuess > 100) {
        result.innerText = 'Введите чсло от 1 до 100'
    } else if (userGuess === randomNumber) {
        result.innerText = 'Поздровляю вы угадали число✅'
        setTimeout(() => {
             randomNumber = Math.floor(Math.random() * 100) + 1
             result.innerText = 'Загаданное число обновлено! Угадайте снова😘'
        }, 3000);
    } else if(userGuess < randomNumber) {
        result.innerText = 'Загадачное число больше⬆️'
    } else {
        result.innerText = 'Загадачное число меньше⬇️'
    }
})































































































































































































































































